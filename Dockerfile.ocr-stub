FROM golang:1.26 AS build
WORKDIR /src
COPY go.mod go.sum ./
RUN go mod download
COPY . .
RUN CGO_ENABLED=0 go build -o /ocr-stub ./cmd/ocr-stub

FROM gcr.io/distroless/static-debian12
COPY --from=build /ocr-stub /ocr-stub
ENTRYPOINT ["/ocr-stub"]
