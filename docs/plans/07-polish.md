# Sub-plan 7 — Polish (optional, à la carte)

`POST /documents/{id}/reextract` (new `extractions` row, replace aggregate); signed GCS URLs for page images in the status DTO; `cmd/worker` split for the poller; GitHub Actions CI (vet/test/compose e2e); `.air.toml` live reload; README with architecture diagram; API-key auth middleware.

---

